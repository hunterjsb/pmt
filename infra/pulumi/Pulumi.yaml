name: pmt
description: pmt AWS infrastructure (Cognito, Lambda pmproxy)
runtime:
  name: python
  options:
    virtualenv: venv
backend:
  url: s3://pmt-pulumi-state-350985642081-euw1
config:
  aws:region:
    description: AWS region (pinned to eu-west-1 for Polymarket co-location)
    value: eu-west-1
